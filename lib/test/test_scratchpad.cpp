#include <string>
#include <cstdio>
/**
 * This test is intentionally left blank. It works as a C++ scratchpad.
 * Just add code here as needed and run make check to execute it.
 * Any output can be found in compiled/test_scratchpad.log
 */
int main() {
    std::string test = "Testing!";
    if (test == "Testing!") {
        puts("You CAN use == on strings in C++");
    }
}
